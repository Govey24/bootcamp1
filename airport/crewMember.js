const Bag = require('./bags')
const Plane = require('./plane')

class crewMember {
    name; position; staffNumber;
}

module.exports = crewMember;

const johnSmith = new crewMember;

console.log(crewMember);